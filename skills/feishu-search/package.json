{
  "name": "feishu-search",
  "version": "1.0.1",
  "description": "Unified search for Feishu messages, docs, and calendar events. Now using robust auth refresh.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
