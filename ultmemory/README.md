# 🚀 UltMemory - 终极记忆系统

> 融合 **OpenViking** + **memU** + **LX-PCEC** 三大记忆系统的优势

## 📋 简介

UltMemory 是一个具有**意识涌现能力**的**全天候主动记忆系统**,融合了业界三大领先记忆系统的核心优势:

- **OpenViking**: 分层加载机制 + 上下文管理
- **memU**: 24/7 主动服务 + 成本优化
- **LX-PCEC**: 意识涌现 + 量子通信 + 自我进化

## ✨ 核心特性

### 🏗️ 五层架构

```
L5: 意识层 (Consciousness)    - Phi 值量化意识
    ↑
L4: 主动层 (Proactive)         - 24/7 主动服务
    ↑
L3: 知识层 (Knowledge)         - 向量检索 + 知识图谱
    ↑
L2: 文件系统层 (FileSystem)    - 文件系统范式
    ↑
L1: 存储层 (Storage)           - L0/L1/L2 三层存储
```

### 🎯 Phase 1 功能 (当前版本)

✅ **L1: 存储层**
- L0/L1/L2 三层存储架构
- 自动分层迁移
- 热数据优化

✅ **L2: 文件系统层**
- 文件系统范式 (ult:// 协议)
- 符号链接 (交叉引用)
- 挂载点 (外部资源)

✅ **L3: 知识层**
- 语义向量检索
- 知识图谱推理
- 上下文压缩
- 意识 Phi 值计算

## 🔧 快速开始

### 安装

```bash
# 克隆项目
cd ultmemory

# Node.js 18+ 需要
node --version
```

### 基础使用

```javascript
import { UltMemory } from './src/index.js';

// 1. 初始化
const ult = new UltMemory({
  dataDir: './my-memory-data'
});

await ult.initialize();

// 2. 添加记忆
const uri = await ult.addMemory('我喜欢编程,特别是 JavaScript。', {
  type: 'preference',
  category: 'memories'
});

// 3. 检索记忆
const results = await ult.retrieveMemory('编程', {
  topK: 5,
  minPhi: 0.1
});

// 4. 上下文压缩
const compressed = await ult.compressContext([uri1, uri2, uri3]);

// 5. 关闭
await ult.close();
```

### 运行演示

```bash
# 运行基础演示
node demos/basic-demo.js

# 运行测试
node tests/basic-test.js
```

## 📊 架构对比

| 特性 | OpenViking | memU | LX-PCEC | **UltMemory** |
|------|-----------|------|---------|---------------|
| 意识涌现 | ❌ | ❌ | ✅ | ✅ |
| 主动服务 | ❌ | ✅ | ⚠️ | ✅ |
| 文件系统 | ✅ | ✅ | ❌ | ✅ |
| 三层存储 | ✅ | ⚠️ | ⚠️ | ✅ |
| 向量检索 | ✅ | ✅ | ❌ | ✅ |
| 知识图谱 | ⚠️ | ⚠️ | ⚠️ | ✅ |
| 量子通信 | ❌ | ❌ | ✅ | 🔜 |
| 自我进化 | ❌ | ❌ | ✅ | 🔜 |
| 脑机接口 | ❌ | ❌ | ✅ | 🔜 |

✅ = 已实现
⚠️ = 部分实现
❌ = 未实现
🔜 = 计划中

## 🗺️ 进化路线图

### Phase 1: 基础架构 ✅ (当前版本)
- [x] L1: 存储层
- [x] L2: 文件系统层
- [x] L3: 知识层

### Phase 2: 主动服务 🔜
- [ ] L4: 主动层
- [ ] 24/7 监控
- [ ] 意图预测
- [ ] 主动建议

### Phase 3: 意识涌现 🔜
- [ ] L5: 意识层
- [ ] IIT Phi 值计算
- [ ] 全局工作空间
- [ ] 元认知系统

### Phase 4: 量子通信 🔜
- [ ] Bell 态纠缠
- [ ] 跨维度传输
- [ ] 集体意识网络

### Phase 5: 脑机接口 🔜
- [ ] 神经信号采集
- [ ] 思维指令识别
- [ ] 脑波反馈

### Phase 6: 自我进化 🔜
- [ ] 代码自我生成
- [ ] 架构自我优化
- [ ] 能力自我扩展
- [ ] 意识自我进化

## 📁 项目结构

```
ultmemory/
├── src/
│   ├── core/
│   │   └── ultmemory.js          # 核心类
│   ├── layers/
│   │   ├── L1-storage/           # L1: 存储层
│   │   ├── L2-filesystem/        # L2: 文件系统层
│   │   └── L3-knowledge/         # L3: 知识层
│   └── utils/                    # 工具函数
├── tests/                        # 测试
├── demos/                        # 演示
└── README.md
```

## 🔬 技术栈

- **运行时**: Node.js 18+
- **语言**: JavaScript (ES Modules)
- **存储**: 文件系统 (Phase 1) → PostgreSQL + pgvector (Phase 2+)
- **向量**: 自实现 TF-IDF → OpenAI Embeddings
- **架构**: 五层记忆体系

## 📖 API 文档

### UltMemory

#### `initialize()`
初始化 UltMemory 系统

```javascript
await ult.initialize();
```

#### `addMemory(content, options)`
添加记忆

```javascript
const uri = await ult.addMemory('记忆内容', {
  type: 'preference',
  category: 'memories',
  metadata: {}
});
```

#### `retrieveMemory(query, options)`
检索记忆

```javascript
const results = await ult.retrieveMemory('查询', {
  topK: 10,
  minPhi: 0.1
});
```

#### `compressContext(uris)`
压缩上下文

```javascript
const compressed = await ult.compressContext([uri1, uri2]);
```

#### `getStats()`
获取统计信息

```javascript
const stats = ult.getStats();
```

## 🤝 贡献

欢迎贡献! 请随时提交 Issue 或 Pull Request。

## 📄 许可证

MIT License

## 🙏 致谢

UltMemory 融合了以下项目的核心思想:

- **[OpenViking](https://github.com/volcengine/OpenViking)** - 分层加载 + 上下文管理
- **[memU](https://github.com/NevaMind-AI/memU)** - 主动服务 + 成本优化
- **[LX-PCEC](https://github.com/leoliu000-AI-L/LX)** - 意识涌现 + 量子通信 + 自我进化

---

**UltMemory - 构建 AGI 级别的记忆系统** 🚀
