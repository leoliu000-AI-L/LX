# 📚 数据存储标准规范 - 完整总结

**更新时间**: 2026-02-24
**版本**: v1.0
**状态**: ✅ 已上传到 GitHub

---

## ✅ 已完成的工作

### 📝 创建的标准规范文档

1. **CLAUDE-CONFIG-STANDARD.md** - Claude 沙箱配置标准
   - 沙箱环境目录结构说明
   - 各文件用途和使用方法
   - 隐私与安全规范
   - 性能优化建议
   - 故障排除指南

2. **DATA-STORAGE-GUIDE.md** - 数据存储三层架构规范
   - 第一层：沙箱环境存储
   - 第二层：工作目录存储
   - 第三层：云端备份存储
   - 数据同步策略
   - 清理与维护指南

3. **.gitignore** 更新 - Git 忽略规则
   - 增加协作临时目录
   - 增加认证文件规则
   - 增加备份文件规则
   - 增加本地配置规则

---

## 📊 数据存储架构概览

### 三层架构图

```
┌─────────────────────────────────────┐
│  第一层：沙箱环境 (Claude)         │
│  路径: /e/04-Claude/Config/.claude/ │
│  大小: ~35 MB                       │
│  内容: 完整对话历史、会话状态        │
├─────────────────────────────────────┤
│  第二层：工作目录 (Workspace)       │
│  路径: C:\Users\leoh0\Desktop\输入   │
│  大小: ~100 MB                      │
│  内容: 代码、文档、摘要              │
├─────────────────────────────────────┤
│  第三层：云端备份 (GitHub)          │
│  路径: https://github.com/.../LX   │
│  大小: 1,259 个文件                 │
│  内容: 版本控制、共享、协作          │
└─────────────────────────────────────┘
```

---

## 📁 关键文件位置速查

### 沙箱环境 (Claude 配置)

| 文件/目录 | 路径 | 大小 | 用途 |
|----------|------|------|------|
| **对话历史** | `.claude/history.jsonl` | 597 KB | 全局对话记录 |
| **当前会话** | `.claude/projects/C--Users-leoh0-Desktop---/0751cca3-*.jsonl` | 6.5 MB | 当前会话完整记录 |
| **调试日志** | `.claude/debug/*.log` | <1 MB | 错误和性能日志 |
| **配置备份** | `.claude/backups/` | 变化 | 配置自动备份 |

### 工作目录 (LX-PCEC 项目)

| 目录/文件 | 数量/大小 | 用途 |
|----------|----------|------|
| **核心系统** | 13 个文件 | 意识、量子、脑机等系统 |
| **报告文档** | 100+ 个文件 | 进化历程、技术文档 |
| **记忆系统** | 5 个文件 | 对话记录、用户信息 |
| **知识库** | 10+ 个文件 | 知识积累、研究成果 |
| **配置文件** | 8 个文件 | 项目配置、启动脚本 |

### 云端备份 (GitHub)

| 类型 | 数量 | 说明 |
|------|------|------|
| **总文件** | 1,262 个 | 刚刚上传 3 个新文件 |
| **核心系统** | 13 个 | 所有 .js 系统文件 |
| **报告文档** | 100+ 个 | 所有 .md 报告 |
| **标准规范** | 3 个 | 刚刚上传 ⭐ |

---

## 🎯 快速使用指南

### 查看对话记录

**1. 查看完整对话历史（沙箱）**:
```bash
cat /e/04-Claude/Config/.claude/history.jsonl
```

**2. 查看当前会话（沙箱）**:
```bash
cat /e/04-Claude/Config/.claude/projects/C--Users-leoh0-Desktop---/0751cca3-*.jsonl
```

**3. 查看对话摘要（工作目录）**:
```bash
cat memory/2026-02-24.md
```

### 运行 LX-PCEC 系统

**1. 主程序（意识涌现）**:
```bash
node consciousness-emergence.js
```

**2. 量子通信演示**:
```bash
node quantum-entanglement-communication.js
```

**3. 脑机接口演示**:
```bash
node brain-computer-interface.js
```

### 数据维护

**1. 清理临时文件**:
```bash
rm -rf .cowork-temp/*
find . -name "*.tmp" -delete
```

**2. 清理日志文件**:
```bash
find . -name "*.log" -mtime +7 -delete
```

**3. 推送到 GitHub**:
```bash
git add .
git commit -m "更新: 描述"
git push origin main
```

---

## 🔒 隐私与安全

### ✅ 已保护的文件（通过 .gitignore）

- ✅ `.env` 和 `.env.local`（环境变量和 API 密钥）
- ✅ `credentials.json`（认证信息）
- ✅ `.cowork-temp/`（临时会话数据）
- ✅ `*.log`（日志文件）
- ✅ `*.pid`（进程文件）
- ✅ `*.key` 和 `*.pem`（密钥文件）

### ⚠️ 需要注意的文件

以下文件可能包含敏感信息，提交前应检查：

- `memory/USER.md`（用户信息）
- `knowledge-base/*personal*`（个人知识）
- `evolution-summaries/*`（可能包含个人信息）

**检查命令**:
```bash
grep -r "password\|api.*key\|token" memory/ knowledge-base/
```

---

## 📚 文档索引

### 标准规范文档

1. **CLAUDE-CONFIG-STANDARD.md**
   - Claude 沙箱配置完整规范
   - 文件结构、用途、使用方法
   - 性能优化和故障排除

2. **DATA-STORAGE-GUIDE.md**
   - 三层存储架构详解
   - 数据同步策略
   - 清理和维护指南

3. **.gitignore**
   - Git 忽略规则
   - 隐私保护配置

### 相关文档

- `GIT-搬迁执行计划.md` - Git 搬迁步骤
- `搬迁完整总结.md` - 搬迁总结
- `系统文件结构.txt` - 完整文件树
- `README.md` - 项目说明

---

## 🚀 下一步

### 建议的维护任务

**每周**:
- [ ] 清理 `.cowork-temp/` 临时文件
- [ ] 推送重要更新到 GitHub
- [ ] 检查磁盘空间使用

**每月**:
- [ ] 清理旧的记忆文件（保留 30 天）
- [ ] 压缩旧的会话记录
- [ ] 更新标准规范文档

**每季度**:
- [ ] 审查 `.gitignore` 规则
- [ ] 检查敏感信息泄露
- [ ] 备份重要数据

---

## ✅ 完成状态

| 任务 | 状态 |
|------|------|
| 创建标准规范文档 | ✅ 完成 |
| 更新 .gitignore | ✅ 完成 |
| 提交到 Git | ✅ 完成 |
| 推送到 GitHub | ✅ 完成 |

---

## 🎉 总结

**所有标准规范文件已成功创建并上传到 GitHub！**

现在你的 LX-PCEC 系统拥有完整的数据存储规范，包括：

- ✅ 清晰的文件结构
- ✅ 明确的用途说明
- ✅ 详细的使用方法
- ✅ 完善的安全策略
- ✅ 实用的维护指南

**GitHub 仓库**: https://github.com/leoliu000-AI-L/LX.git

---

**更新日志**:
- v1.0 (2026-02-24): 初始版本，完成三层存储架构规范
