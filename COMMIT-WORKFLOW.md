# Git提交工作流

## 📋 每次进化后的标准提交流程

### 1. 查看当前状态
```bash
git status
```

### 2. 查看具体更改
```bash
# 查看未暂存的更改
git diff

# 查看已暂存的更改
git diff --staged
```

### 3. 添加文件到暂存区
```bash
# 添加所有更改
git add .

# 或选择性添加
git add *.js
git add evolver-main/pcec-history.jsonl
git add evomap/.published-assets.json
```

### 4. 创建提交

#### 提交消息格式
```bash
git commit -m "🧬 PCEC Cycle #[N]: [简短描述]

## 新增
- 新增资产: [资产名称]
- 功能: [功能描述]

## 优化
- 性能: [优化描述]
- 代码: [代码改进]

## 修复
- Bug: [修复描述]

## 数据
- 发布记录: 更新到 X 个
- PCEC周期: 记录 #N
- 进化候选: Y 个

---
状态: 🟢 成功 / 🟡 部分 / 🔴 失败
成功率: XX%"
```

#### 实际示例
```bash
git commit -m "🧬 PCEC Cycle #25: 发布智能资产包

## 新增
- 资产: 元学习框架、递归式自我改进、涌现行为检测
- 脚本: publish-next-gen.js

## 数据
- 发布记录: 32 → 35
- PCEC周期: 完成 #25
- 验证成功率: 59.4% → 62.5%

---
状态: 🟢 成功
发布: 3/3"
```

### 5. 推送到GitHub
```bash
git push origin main
```

## 🏷️ 版本标签

### 创建版本标签
```bash
# 重要里程碑时创建标签
git tag -a v2.1.0 -m "PCEC v2.1.0 - Advanced AI Integration

- 新增: 元学习、神经架构搜索
- 优化: 发布速度提升30%
- 修复: 速率限制处理

发布资产: 25个
PCEC周期: 28个"

git push origin v2.1.0
```

### 查看所有标签
```bash
git tag
git show v2.1.0
```

## 🔄 回退操作

### 查看提交历史
```bash
# 简洁查看
git log --oneline --all --graph

# 详细查看
git log -p -2

# 查看特定文件历史
git log --follow -- FILE_PATH
```

### 回退方式

#### 方式1: Soft reset (保留更改)
```bash
# 撤销最后一次提交，但保留更改在暂存区
git reset --soft HEAD~1
```

#### 方式2: Hard reset (完全回退)
```bash
# 回退到指定提交（危险！会丢失未提交的更改）
git reset --hard <commit-hash>
```

#### 方式3: Revert (推荐)
```bash
# 创建新提交来撤销指定提交（保留历史）
git revert <commit-hash>
```

### 从回退中恢复
```bash
# 查看所有操作日志
git reflog

# 恢复到之前的状态
git reset --hard HEAD@{N}
```

## 📊 分支管理

### 创建开发分支
```bash
# 创建新分支进行实验
git checkout -b feature/advanced-ai

# 在分支上进行开发...

# 合并回主分支
git checkout main
git merge feature/advanced-ai
```

### 查看所有分支
```bash
git branch -a
```

## 🔍 数据文件管理

### 关键数据文件（必须提交）
- `evomap/.published-assets.json` - 发布记录
- `evolver-main/pcec-history.jsonl` - PCEC历史
- `evolver-main/assets/gep/candidates.jsonl` - 进化候选
- `evolver-main/assets/gep/events.jsonl` - 进化事件
- `evolver-main/assets/gep/genes.jsonl` - Gene定义

### 不应提交的文件（.gitignore）
- `*.log` - 日志文件
- `.pcec-monitor-state.json` - 运行时状态
- `node_modules/` - 依赖包

## 📝 提交最佳实践

### DO ✅
- 每次PCEC周期后提交
- 使用清晰的提交消息
- 包含数据文件的更新
- 创建版本标签标记里程碑
- 提交前先 `git status` 检查

### DON'T ❌
- 不要提交敏感信息（API密钥等）
- 不要提交过大的二进制文件
- 不要在主分支上做实验
- 不要忘记推送重要更改

## 🚀 自动化提交（可选）

创建自动提交脚本 `auto-commit.sh`:
```bash
#!/bin/bash
DATE=$(date +%Y%m%d-%H%M%S)
git add .
git commit -m "🤖 Auto-commit: $DATE

PCEC自动进化周期
---
状态: 自动提交"
git push origin main
```

然后在PCEC监控器中调用。

---

**记住**: 每次提交都是一次快照，确保进化可追溯、可回退！
